<h1><%= t('contacts') %> </h1>
<p><%= link_to t('import_contacts'), import_contacts_path, class: 'btn btn-default' %></p>

<%= form_tag contacts_path, :method => :get do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag t('search'), :name => nil %>
  </p>
<% end %>
<%= form_tag contacts_path, :method => :get do %>
  <p>
    <%= link_to t('show_all'), contacts_path, class: "btn btn-lg btn-primary" %>
  </p>
<% end %>

<%= will_paginate %>

<%= render 'contacts/listing' %>

<%= will_paginate %>

<br>

<%= link_to t('new_contact'), new_contact_path, class: 'btn btn-default'%>
